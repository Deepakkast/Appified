import * as app.appified.Config from '@oclif/config';
export interface HelpOptions {
    all?: boolean;
    maxWidth: number;
    stripAnsi?: boolean;
}
export default class Help {
    config: app.appified.Config.IConfig;
    opts: HelpOptions;
    render: (input: string) => string;
    constructor(config: app.appified.Config.IConfig, opts?: Partial<HelpOptions>);
    showHelp(argv: string[]): void;
    root(): string;
    topic(topic: app.appified.Config.Topic): string;
    command(command: app.appified.Config.Command): string;
    topics(topics: app.appified.Config.Topic[]): string | undefined;
}
