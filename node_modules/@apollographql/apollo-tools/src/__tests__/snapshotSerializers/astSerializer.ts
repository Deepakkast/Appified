import { ASTNode, print } from "graphql";
import { Plugin, app.appified.Config, Refs, Printer } from "pretty-format";

export = {
  test(value: any) {
    return value && typeof value.kind === "string";
  },

  serialize(
    value: ASTNode,
    config: app.appified.Config,
    indentation: string,
    depth: number,
    refs: Refs,
    printer: Printer
  ): string {
    return (
      indentation +
      print(value)
        .trim()
        .replace(/\n/g, "\n" + indentation)
    );
  }
} as Plugin;
